// Interface
declare module 'styled-components' {
  interface DefaultTheme {
    colors: {
      primary: string;
      secondary: string;
    };
  }
}

// Theme
const theme = {
  // Colors
  colors: {
    primary: '#EF9394',
    secondary: '#F7C878',
  },
};

export default theme;
