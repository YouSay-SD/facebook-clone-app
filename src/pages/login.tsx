import React, { FC } from 'react';
import { Container, AuthContent } from '../components';

const Login: FC = () => {
  return (
    <Container>
      <AuthContent />
    </Container>
  );
};

export default Login;
