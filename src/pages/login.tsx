import React from 'react';
import {
  Container,
  LoginContent,
} from '../components';

const Login: React.FC = () => {
  return (
    <Container>
      <LoginContent />
    </Container>
  );
};

export default Login;
