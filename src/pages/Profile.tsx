import React, { FC } from 'react';
import { Hero, NavbarProfile } from '../components';

const Profile: FC = () => {
  return (
    <>
      <Hero />
      <NavbarProfile />
    </>
  );
};

export default Profile;
