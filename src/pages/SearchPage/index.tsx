import React, { FC } from 'react';
import { SearchContent } from '../../components';

const SearchPage: FC = () => {
  return (
    <>
      <SearchContent />
    </>
  );
};

export default SearchPage;
