import React, { FC } from 'react';
import { Container, AuthContent } from '../../components';

const LoginPage: FC = () => {
  return (
    <Container>
      <AuthContent />
    </Container>
  );
};

export default LoginPage;
